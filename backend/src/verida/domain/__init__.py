"""Domain layer — pure Python entities.

No external framework dependencies are imported here.
These are the canonical data structures that the rest of the system
references via the ``domain`` package.
"""
